# Rebuild and restart when Go files change
**/*.go {
    prep: go build -o ./tmp/main .
    daemon +sigterm: ./tmp/main
}

# Regenerate when port interfaces change
internal/domain/ports/**/*.go {
    prep: go generate ./...
}
